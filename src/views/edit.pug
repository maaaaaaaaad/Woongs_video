extends base.pug

block content
  form(method="POST") 
    input(type="text", name="title", value=`title`,
          placeholder="Video Title", autocomplete="off",required)
    input(type="submit", value="Save")